{
  "name": "cloudsync-panel",
  "version": "1.0.1",
  "description": "CloudSync Panel - painel que envia credenciais ao servidor para automatizar criação de VMs (use com responsabilidade).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prestart": "node -v && npm -v"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.0",
    "helmet": "^7.0.0",
    "puppeteer": "^21.3.8"
  }
}
